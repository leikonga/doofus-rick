# doofus-rick

Because it can't be worse than this.

## Installation

1. Install Go 1.26+
2. Run `go mod download`

Use `cp .env.example .env`, set the required environment variables, then you can run the application with
`go run ./cmd/doofus-rick`

## License

[MIT](/LICENSE)
